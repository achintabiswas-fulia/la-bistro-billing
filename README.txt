LA BISTRO BILLING WEB APP

This package is prepared for HTTPS hosting (for example GitHub Pages).
After it is hosted, open the site in Chrome on Android. Chrome can then offer
Add to Home screen / Install app because it is a web app rather than a local HTML file.

Important: this web version is for the billing interface. Direct Bluetooth ESC/POS
printing still belongs in the native Android app.
